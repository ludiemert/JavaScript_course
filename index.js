let olderPersonName = prompt("Enter the oldest person's name")
let olderPersonAge = prompt("Enter the oldest person's age")

let youngerPersonName = prompt("Enter the younger person's name")
let youngerPersonAge = prompt("Enter the younger person's age")

let ageDifference = olderPersonAge - youngerPersonAge

alert (
         "Person oldest: " + olderPersonName + "\nAge: " + olderPersonAge +
        "\n\nPerson younger " + youngerPersonName + "\nAge: " + youngerPersonAge +
        "\n\nDifference the Age: " + ageDifference
)